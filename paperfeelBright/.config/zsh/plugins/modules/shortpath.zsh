#-----------------------------------------------------------------------------------------------
# SHORTPATH
#-----------------------------------------------------------------------------------------------
shortpath() {
  path="${1:-$PWD}"
  IFS='/' read -r -A parts <<< "${path/#$HOME/~}"
  len=${#parts[@]}
  echo "${parts[$((len-2))]}/${parts[$((len-1))]}/${parts[$len]}"
}

