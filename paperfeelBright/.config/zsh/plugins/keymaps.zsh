#-----------------------------------------------------------------------------------------------
# KEYMAPS
#-----------------------------------------------------------------------------------------------
bindkey -M vicmd 'u' undo
bindkey -M viins 'jk' vi-jk-escape
bindkey -M vicmd 'v' enter-visual-mode
bindkey -M vicmd 'i' exit-visual-wrapper
